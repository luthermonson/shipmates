flowchart LR
    subgraph Ship[Ship]
        Local[Project local server]
        Projection[Bounded read-only projection]
        Local --> Projection
    end
    Projection -->|outbound authenticated tunnel| Observer[Fleet observer service]
    Observer --> UI[Read-only browser UI]
    Observer --> API[Snapshot, roster, events, follow]
    Steer[Scoped steer client] -->|exact active turn| Observer
    Interrupt[Scoped interrupt client] -->|exact active turn| Observer
    Observer --> Audit[Sanitized immutable audit]
