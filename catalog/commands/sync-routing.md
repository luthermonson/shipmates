---
description: Load this project's routing conventions (claim / worktree / PR / verdict / cleanup) into the current session. Run at session start when working as a fleet crew member, or any time you want to re-pull the rules mid-session.
---

Run `shipmates routing show` and adopt the routing conventions it prints for the
rest of this session — they govern how you claim work, branch, open PRs, review,
and merge in this project.

If the command errors (no routing is configured), say so briefly and proceed
without them — they're optional.
